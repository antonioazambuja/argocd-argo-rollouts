apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  labels:
    serviceName: rollouts-demo
  name: rollouts-demo
spec:
  generators:
  - clusters:
      selector:
        matchExpressions:
        - key: cluster-name
          operator: In
          values:
          - in-cluster
  goTemplate: true
  goTemplateOptions:
  - missingkey=error
  template:
    metadata:
      finalizers:
      - resources-finalizer.argocd.argoproj.io
      labels:
        chart: ifood-app
        cluster-name: '{{ .name }}'
        environment: sandbox
        serviceName: rollouts-demo
      name: rollouts-demo-{{ .name | sha256sum | trunc 5 }}
    spec:
      ignoreDifferences:
      - group: '*'
        kind: '*'
        managedFieldsManagers:
        - rollouts-controller
      - group: argoproj.io
        jsonPointers:
        - /spec/replicas
        kind: Rollout
      project: default
      destination:
        name: '{{ .name }}'
        namespace: default
      sources:
      - ref: values
        repoURL: https://github.com/antonioazambuja/argocd-argo-rollouts.git
        targetRevision: main
        path: values/
      syncPolicy:
        syncOptions:
        - ApplyOutOfSyncOnly=false
        - ServerSideApply=true
status: {}
